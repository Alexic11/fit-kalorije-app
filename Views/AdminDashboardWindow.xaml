<Window x:Class="Fit.Views.AdminDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource AdminPanelTitle}"
        Height="500"
        Width="800"
        Background="#f5f5f5"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Border Width="400"
                Height="300"
                Background="White"
                CornerRadius="12"
                Padding="30"
                BorderBrush="#e0e0e0"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                SnapsToDevicePixels="True"
                >
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{DynamicResource AdminWelcomeText}"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#333"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <Button x:Name="btnPregledKorisnika"
                        Content="{DynamicResource ViewUsersButton}"
                        Width="220"
                        Height="45"
                        Background="#007ACC"
                        Foreground="White"
                        FontWeight="SemiBold"
                        FontSize="15"
                        Cursor="Hand"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Margin="0,10,0,10"
                        />
            </StackPanel>
        </Border>
    </Grid>
</Window>
